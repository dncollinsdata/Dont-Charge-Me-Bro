# expo-router pulls react-dom@19.2.8, whose peer range is react ^19.2.8, while
# Expo SDK 57 pins react to 19.2.3. react-dom is web-only and never loads in this
# iOS app, so react stays on the SDK-expected version and peers are relaxed.
legacy-peer-deps=true
